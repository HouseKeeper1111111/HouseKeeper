<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:icon="@mipmap/ic_launcher"
        android:shortcutId="reminderslist"
        android:shortcutShortLabel="@string/shortcut_reminders_list"
        tools:ignore="UnusedAttribute"> <!-- Shortcuts only on API 25+ -->
        <!--suppress AndroidDomInspection -->
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="com.example.myapplication.ui.reminderslist.RemindersListActivity"
            android:targetPackage="felixwiemuth.simplereminder.dev" /> <!-- NOTE: Must be set to application ID -->
    </shortcut>

</shortcuts>